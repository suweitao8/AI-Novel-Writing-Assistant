# Frontend optional overrides
# 大多数本地开发场景不需要这个文件。
# 开发端口固定：服务端 3100（server/.env）、前端 5174；API 默认走同源 /api 代理。
# 如果不显式配置且需要直连 API，地址是：
# http(s)://当前页面 hostname:3100/api
# 如果你通过局域网地址访问页面，不要把 API 固定成 localhost，
# 否则浏览器会去请求“当前访问设备自己的 localhost:3100”。

# 只有在你明确要固定 API 地址时才取消注释：
# VITE_API_BASE_URL=http://localhost:3100/api
# 默认 10 分钟，可按需要调大。
VITE_API_TIMEOUT_MS=600000

# 故事资产展示组件

本目录统一角色、场景、道具这三类故事设定资产在脚本侧和设定中心的卡片与详情弹窗。

- `storyAssetPresentation.ts`：把 API 资产转换成稳定的展示模型，不发起请求。
- `StoryAssetCard.tsx`：统一卡片结构和点击详情行为。
- `StoryAssetDetailDialog.tsx`：统一只读详情、状态信息和操作区；编辑表单、删除请求由调用方注入。

角色库里的图片灯箱、图片生成确认弹窗和分镜首帧预览属于媒体/镜头流程，不使用本目录。

# 工作树生命周期审计与孤立目录恢复 Implementation Plan

## Task 1: 先提交设计契约

- [x] 新增设计文档，明确审计、深层依赖链接检查和显式恢复的边界。
- [ ] 以 signed commit 提交设计文档后再进入实现。

## Task 2: 扩展文件系统隔离检查

- [ ] 将依赖入口检查改为递归 `lstat`/`realpath`，覆盖非 scoped package 的深层链接。
- [ ] 为深层外部链接、内部链接和循环链接补充测试。

## Task 3: 增加生命周期审计

- [ ] 新增登记状态、`prunable` 状态、分支未登记路径和异常汇总模块。
- [ ] 增加 `pnpm workflow:audit` 只读入口和测试。
- [ ] 将生命周期断言接入创建和集成前置条件。

## Task 4: 增加显式孤立目录恢复

- [ ] 新增 Git 树逐文件一致性比较和固定依赖根复用逻辑。
- [ ] 增加默认 dry-run 与 `--apply` 恢复命令及测试。
- [ ] 对缺失/新增源码、未合入分支、外部链接和非精确路径全部拒绝。

## Task 5: 文档、验证和交付

- [ ] 更新工作流 Wiki 的异常审计和恢复规则。
- [ ] 运行聚焦测试、`pnpm test:workflow`、文档检查和 diff 检查。
- [ ] 通过受控集成入口合入并推送，确认主线、远端和活动工作树状态。
